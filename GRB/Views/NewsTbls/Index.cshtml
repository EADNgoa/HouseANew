@model IEnumerable<GoaRehabWeb.Models.NewsTbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.N_Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.N_Desc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.N_Pic)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.N_Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.N_Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.N_Desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.N_Pic)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.N_Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.N_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.N_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.N_Id })
        </td>
    </tr>
}

</table>
