@model IEnumerable<GRB.Models.AnnouncementsTbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.A_Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.A_Desc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.A_Pic)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.A_Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.A_Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.A_Desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.A_Pic)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.A_Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.A_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.A_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.A_Id })
        </td>
    </tr>
}

</table>
