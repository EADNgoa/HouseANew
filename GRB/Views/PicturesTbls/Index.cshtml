@model IEnumerable<GoaRehabWeb.Models.PicturesTbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pic_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pic_Path)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pic_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pic_Path)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pic_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Pic_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Pic_Id })
        </td>
    </tr>
}

</table>
